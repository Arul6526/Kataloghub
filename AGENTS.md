# Agent Rules & Guidelines

- **Build Verification Preference**: Do NOT automatically run `npm run build` or background build commands unless explicitly requested by the user. The user will execute build commands manually.

## Agent skills

### Issue tracker

Issues are tracked as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context repository layout. See `docs/agents/domain.md`.
